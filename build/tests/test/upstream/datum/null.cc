// auto-generated by yaml_to_cxx.py from test/upstream/datum/null.yaml
#include "testlib.h"
void test_upstream_datum_null() {
    enter_section("test_upstream_datum_null: Tests of conversion to and from the RQL null type");
    TEST_EQ(maybe_run(R::expr(R::Nil()), *conn), (R::Nil()));
    TEST_EQ(maybe_run(R::expr(R::Nil()).type_of(), *conn), ("NULL"));
    TEST_EQ(maybe_run(R::expr(R::Nil()).coerce_to("string"), *conn), ("null"));
    TEST_EQ(maybe_run(R::expr(R::Nil()).coerce_to("null"), *conn), (R::Nil()));
    exit_section();
}
