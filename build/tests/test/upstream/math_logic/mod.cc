// auto-generated by yaml_to_cxx.py from test/upstream/math_logic/mod.yaml
#include "testlib.h"
void test_upstream_math_logic_mod() {
    enter_section("test_upstream_math_logic_mod: Tests for the basic usage of the mod operation");
    TEST_EQ(maybe_run((R::expr(10) % 3), *conn), (1));
    TEST_EQ(maybe_run((R::expr(10) % R::expr(3)), *conn), (1));
    TEST_EQ(maybe_run(R::expr(10).mod(3), *conn), (1));
    TEST_EQ(maybe_run((R::expr(-10) % -3), *conn), (-1));
    TEST_EQ(maybe_run((R::expr(4) % "a"), *conn), (err("ReqlQueryLogicError", "Expected type NUMBER but found STRING.", R::Array{1})));
    TEST_EQ(maybe_run((R::expr("a") % 1), *conn), (err("ReqlQueryLogicError", "Expected type NUMBER but found STRING.", R::Array{0})));
    TEST_EQ(maybe_run((R::expr("a") % "b"), *conn), (err("ReqlQueryLogicError", "Expected type NUMBER but found STRING.", R::Array{0})));
    exit_section();
}
