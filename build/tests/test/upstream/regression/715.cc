// auto-generated by yaml_to_cxx.py from test/upstream/regression/715.yaml
#include "testlib.h"
void test_upstream_regression_715() {
    enter_section("test_upstream_regression_715: 715 -- noreply writes");
    temp_table tbl_table;
    R::Query tbl = tbl_table.table();
    TEST_EQ(tbl.insert(R::object())["inserted"].run(*conn), (1));
    TEST_EQ(tbl.insert(R::object())["inserted"].run(*conn), (1));
    TEST_EQ(tbl.insert(R::object()).run(*conn), (R::Nil()));
    TEST_EQ(tbl.count().run(*conn), (3));
    exit_section();
}
